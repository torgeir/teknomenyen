name: test redirect
on:
  workflow_dispatch:
  push:
    branches:
      - "master"
jobs:
  build:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        run: | 
          echo $(echo "hei") > file
          cat file
